export const capitalize = (str) => {
  return str[0].toUpperCase() + str.substring(1)
}

// 'c'.toUpperCase()
// "C"

// 'hello'.substring(1)
// "ello"

// 'hello'.substring(0)
// "hello"

// 'hello'.substring(2)
// "llo"
